Tu as accès au serveur MCP cobol-explorer qui expose le graphe de dépendances d'un patrimoine COBOL (assurance GenApp). Utilise ces outils plutôt que de lire les fichiers.

Question : si je modifie le copybook LGPOLICY, quels programmes et quelles chaînes batch (ordonnanceur) sont impactés ? Donne la liste exhaustive, avec preuve fichier:ligne.

Appelle exactement ces outils, dans cet ordre :

graph_lookup(op="impact", node="LGPOLICY") → renvoie la liste exhaustive des programmes + des chaînes batch impactés (clôture transitive, déterministe — ce que la simple lecture ne peut pas garantir).
Pour prouver un COPY, read_source_lines(file="lgipol01.cbl", start=55, end=55) → tu obtiens la ligne exacte COPY LGPOLICY.
Si un nom t'est inconnu, search_code(query="…").
Réponds uniquement à partir des sorties de ces outils et cite chaque fichier:ligne. Ne complète pas de mémoire.